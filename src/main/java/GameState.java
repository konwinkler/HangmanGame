/**
 * Symbolizes the state the game can be in. A state before ongoing (like start) has been omitted for now
 * because with creation the game is ready for input and therefore running.
 */
public enum GameState
{
  ONGOING, WIN, LOSE
}
